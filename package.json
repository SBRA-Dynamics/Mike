{
  "name": "jarvis-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Jarvis server \u2014 transport and sessions (PRD 1), MCP tool surface (PRD 2), Jarvis and workers (PRD 3)",
  "scripts": {
    "start": "node server.js",
    "test": "node test/run.mjs && node test/crash-recovery.mjs && node test/mcp.mjs && node test/prd3.mjs",
    "test:live": "node test/mcp-live.mjs && node test/e2e-live.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
