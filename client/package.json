{
  "name": "mike-client",
  "private": true,
  "version": "0.3.31",
  "type": "module",
  "description": "Mike client \u2014 Even Hub plugin: lens view + companion view (PRD 4)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node -e \"require('fs').copyFileSync('app.json','../public/app.json')\"",
    "preview": "vite preview",
    "pack": "node -e \"const a=require('./app.json'),p=require('./package.json');if(a.version!==p.version){console.error('app.json '+a.version+' != package.json '+p.version);process.exit(1)}console.log('packing '+a.package_id+' '+a.version)\" && npm run build && evenhub pack app.json ../public -o ../mike.ehpk"
  },
  "dependencies": {
    "@evenrealities/even_hub_sdk": "^0.0.15",
    "@evenrealities/pretext": "^0.1.4",
    "jsqr": "^1.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^5.4.0"
  }
}
